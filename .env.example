# Spotify-Deck config
# Client ID is not a secret, but we keep it out of git so you can reuse the repo safely.
VITE_SPOTIFY_CLIENT_ID=YOUR_CLIENT_ID_HERE

# Must EXACTLY match the Redirect URI in Spotify Developer Dashboard
# GitHub Pages example:
# https://YOUR_GITHUB_USERNAME.github.io/Spotify-Deck/
VITE_SPOTIFY_REDIRECT_URI=https://YOUR_REDIRECT_URI_HERE/

# Optional: reduce CPU/network usage
VITE_POLL_INTERVAL_MS=2500
